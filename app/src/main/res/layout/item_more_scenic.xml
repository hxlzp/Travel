<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="80dp">
    <android.support.v7.widget.CardView
        android:id="@+id/card_scenic_item"
        android:layout_width="120dp"
        android:layout_height="80dp"
        android:layout_margin="10dp"
        app:cardBackgroundColor="@color/gray_translucent"
        app:cardCornerRadius="8dp">
        <ImageView
            android:id="@+id/iv_more_scenic_item"
            style="@style/both_match_style"
            android:scaleType="fitXY"
            android:layout_alignParentTop="true"
            android:layout_alignParentStart="true" />
    </android.support.v7.widget.CardView>
    <TextView
        android:id="@+id/tv_scenic_title"
        android:textColor="@color/black"
        android:textSize="16sp"
        android:layout_marginTop="18dp"
        android:layout_marginLeft="10dp"
        android:layout_toRightOf="@+id/card_scenic_item"
        style="@style/both_wrap_style" />
    <TextView
        android:id="@+id/tv_scenic_info"
        android:textColor="@color/themeColor"
        android:textSize="13sp"
        android:layout_toRightOf="@+id/card_scenic_item"
        android:layout_alignBottom="@+id/card_scenic_item"
        android:layout_marginLeft="10dp"
        android:layout_marginBottom="10dp"
        style="@style/both_wrap_style"/>
</RelativeLayout>